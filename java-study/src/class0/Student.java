package class0;

// 클래스 = 설계도 : 객체를 만들기 위한 설계도 [ 붕어빵 틀(클래스) - 붕어빵(생성된 객체, 인스턴스) ]
// 인스턴스 : 설계도 대로 생성된 객체
public class Student {
    // 1. 필드 : 클래스에 정의한 변수, 데이터 항목을 가리키는 용어, 멤버 변수라고도 부름
    String name;
    int score;
    int age;

    // 2. 생성자 : 객체의 초기화를 담당

    // 3. 메서드 : 객체의 동작을 담당. 실행 코드를
}
